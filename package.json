{
  "name": "devflow",
  "private": true,
  "workspaces": ["client", "shared"],
  "scripts": {
    "dev": "concurrently \"cd server-py && uvicorn devflow.main:app --reload --port 4000\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=shared && npm run build --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.4.0"
  }
}
